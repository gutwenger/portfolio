import 'package:flutter/material.dart';

class LandingSubTitle extends StatelessWidget {
  const LandingSubTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Flutter developer", style: TextStyle(fontSize: 40, fontWeight: FontWeight.bold));
  }
}
